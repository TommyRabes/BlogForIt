export default Object.freeze({
    Strings: {
        chunkSplit: {
            NON_POSITIVE_CHUNK_SIZE: 'Unexpected negative or zero chunk size',
            FLOATING_POINT_CHUNK_SIZE: 'Unexpected float number as chunk size'
        }
    }
});